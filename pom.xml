<project xmlns="http://maven.apache.org/POM/4.0.0" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.jlab.coda</groupId>
  <artifactId>jevio</artifactId>
  <version>6.0-SNAPSHOT</version>
  <name>Jefferson Lab EVIO Java Library (JEVIO)</name>
  <description>Java library for EVIO (Event Input/Output)</description>
  <packaging>jar</packaging>

  <!-- Properties for versions -->
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>1.8</maven.compiler.source>   <!-- target Java version -->
    <maven.compiler.target>1.8</maven.compiler.target>
    <lz4.version>1.8.0</lz4.version>
    <disruptor.version>3.4.3</disruptor.version>
    <aha.version>1.0</aha.version>  <!-- AHACompressionAPI is an external jar with no Maven repo -->
    <java.jars.dir>${project.basedir}/java/jars</java.jars.dir>
  </properties>

  <dependencies>
    <!-- LZ4 compression library -->
    <dependency>
      <groupId>org.lz4</groupId>  <!-- Note: modern groupId for lz4-java might be org.lz4 or net.jpountz -->
      <artifactId>lz4-java</artifactId>
      <version>${lz4.version}</version>
    </dependency>
    <!-- LMAX Disruptor (for ring buffer concurrency) -->
    <dependency>
      <groupId>com.lmax</groupId>
      <artifactId>disruptor</artifactId>
      <version>${disruptor.version}</version>
    </dependency>

  <!-- AHA Compression API (third-party, not in Maven Central) -->
  <!-- <dependency>
      <groupId>com.aha.compression</groupId>
      <artifactId>AHACompressionAPI</artifactId>
      <version>${aha.version}</version>
      <scope>system</scope>
      <systemPath>${java.jars.dir}/AHACompressionAPI.jar</systemPath>
  </dependency> -->

    <!-- (Add JUnit dependency here if we have any unit tests) -->
  </dependencies>

  <build>
    <plugins>
      <!-- Maven Compiler Plugin for Java compilation -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <source>${maven.compiler.source}</source>
          <target>${maven.compiler.target}</target>
        </configuration>
      </plugin>


      <!-- Maven Assembly Plugin to create an executable JAR with dependencies -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>3.3.0</version>
        <executions>
          <execution>
            <id>make-assembly</id>
            <phase>package</phase>
            <goals><goal>single</goal></goals>
            <configuration>
              <archive>
                <manifest>
                  <mainClass>org.jlab.coda.hipo.Evio6Converter</mainClass>
                </manifest>
              </archive>
              <descriptorRefs>
                <descriptorRef>jar-with-dependencies</descriptorRef>
              </descriptorRefs>
              <appendAssemblyId>false</appendAssemblyId>
            </configuration>
          </execution>
        </executions>
        <configuration>
          <!-- (global config if needed) -->
        </configuration>
      </plugin>
    </plugins>
  </build>


</project>
